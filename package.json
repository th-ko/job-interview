{
  "name": "interview-question",
  "version": "1.0.0",
  "description": "An sample implementation of an interesting question I got in one of my recent job-interviews",
  "main": "dist/index.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Thomas Kowalski",
  "license": "ISC",
  "devDependencies": {
    "@types/node-fetch": "^2.5.0",
    "typescript": "^3.6.2"
  },
  "dependencies": {
    "node-fetch": "^3.1.1"
  }
}
